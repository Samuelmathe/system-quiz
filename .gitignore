# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/

# Local configs generated by the apps
configuration/config_quiz_pro.json
interface/quiz_board_config.json

# PyInstaller
build/
dist/
*.spec

# Binaries / artifacts
QuizBoardV6

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local Arduino CLI toolchain cache (large, machine-specific)
.arduino-cli/

# Apps hybrides Capacitor : www/ (genere par sync.sh) et les projets natifs
# android/ios (generes par `npx cap add`) ne sont pas commites, regeneres a
# chaque build (local ou CI) depuis web/ -- source de verite unique.
hybrid-app-*/www/
hybrid-app-*/android/
hybrid-app-*/ios/
hybrid-app-*/electron/
hybrid-app-*/node_modules/

# Quiz logger (diagnostics)
diagnostics/quiz_log_*.txt
diagnostics/quiz_resume_*.txt
