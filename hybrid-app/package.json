{
  "name": "quiz-animateur-apk",
  "version": "2.0.0",
  "description": "Application Android APK Hybride pour télécommande Animateur Quiz DMX",
  "scripts": {
    "init:android": "npx cap add android",
    "sync": "npx cap copy && npx cap sync",
    "open": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
